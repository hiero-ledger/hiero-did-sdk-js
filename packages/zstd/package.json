{
  "name": "@hiero-did-sdk/zstd",
  "version": "0.1.7",
  "description": "This package provides Zstandard (Zstd) compression utilities for the Hiero DID SDK JS.",
  "author": {
    "name": "DSR Corporation",
    "url": "https://en.dsr-corporation.com/"
  },
  "bugs": {
    "url": "https://github.com/hiero-ledger/hiero-did-sdk-js/issues"
  },
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "exports": {
    "default": "./dist/index.js"
  },
  "files": [
    "dist",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/hiero-ledger/hiero-did-sdk-js.git",
    "directory": "packages/zstd"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "clean": "rimraf ./dist",
    "compile": "tsup",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "pnpm clean && pnpm compile",
    "format": "prettier --write \"src/**/*.ts\" \"tests/*.ts\"",
    "format-check": "prettier --check \"src/*.ts\" \"tests/*.ts\""
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "zstd-napi": "^0.0.10"
  }
}
